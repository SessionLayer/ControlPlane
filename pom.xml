<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/>
	</parent>
	<groupId>io.sessionlayer</groupId>
	<artifactId>controlplane</artifactId>
	<version>0.0.2</version>
	<name>SessionLayer Control Plane</name>
	<description>SessionLayer Control Plane - API-first control/management plane.</description>
	<url>https://github.com/SessionLayer/ControlPlane</url>
	<licenses>
		<license>
			<name>GPL-3.0-only</name>
			<url>https://www.gnu.org/licenses/gpl-3.0.txt</url>
		</license>
	</licenses>

	<properties>
		<java.version>25</java.version>

		<!--
		  Reproducible-build anchor: a fixed archive timestamp makes the jar
		  byte-identical across builds. release.yml overrides it per tag with the
		  commit date (-Dproject.build.outputTimestamp=<commit ISO>). Honoured by
		  the Apache-parent archiver + the Boot repackage goal.
		-->
		<project.build.outputTimestamp>2025-01-01T00:00:00Z</project.build.outputTimestamp>

		<cyclonedx-maven-plugin.version>2.9.3</cyclonedx-maven-plugin.version>

		<spotless-maven-plugin.version>3.10.0</spotless-maven-plugin.version>
		<maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
		<checkstyle.version>14.0.0</checkstyle.version>
		<openapi-generator.version>7.24.0</openapi-generator.version>

		<!--
		  BouncyCastle (NOT managed by the Spring Boot BOM) for the internal mTLS CA:
		  X.509 CA cert issuance, PKCS#10 CSR parsing +
		  proof-of-possession verification, and server/client leaf issuance (proper
		  KeyUsage / EKU / SAN / BasicConstraints). We do NOT hand-roll X.509 DER and do
		  NOT reuse the SSH OpenSshCertificateAssembler (that stays SSH-only). BC's
		  licence (MIT-style) is GPL-3.0-compatible. Pinned explicitly because the BOM
		  does not manage it.
		-->
		<bouncycastle.version>1.85</bouncycastle.version>

		<!--
		  RE2/J is guaranteed to run in time linear in the input, so a
		  catastrophic-backtracking pattern cannot cause ReDoS on the data-plane
		  RBAC label `regex` operator, which is on the decision hot path. We do
		  NOT use java.util.regex for that operator.
		-->
		<re2j.version>1.8</re2j.version>

		<jqwik.version>1.10.1</jqwik.version>

		<awssdk.version>2.53.2</awssdk.version>

		<azure.sdk.bom.version>1.3.8</azure.sdk.bom.version>

		<!--
		  Override the Boot-managed PostgreSQL JDBC driver (42.7.11) to 42.7.13:
		  42.7.11 is affected by CVE-2026-54291 (GHSA-j92g-9f8w-j867, channel-binding
		  downgrade), fixed in 42.7.12. This is the Flyway-only JDBC driver; the
		  R2DBC runtime driver (r2dbc-postgresql) is a separate artifact.
		-->
		<postgresql.version>42.7.13</postgresql.version>

	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>software.amazon.awssdk</groupId>
				<artifactId>bom</artifactId>
				<version>${awssdk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.azure</groupId>
				<artifactId>azure-sdk-bom</artifactId>
				<version>${azure.sdk.bom.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-tracing-bridge-otel</artifactId>
		</dependency>
		<dependency>
			<groupId>io.opentelemetry</groupId>
			<artifactId>opentelemetry-exporter-otlp</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-client</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webclient</artifactId>
		</dependency>

		<!--
		  Spring gRPC binds any BindableService bean to a standalone Netty gRPC
		  server; the CP overrides Boot's default plaintext listener with its own
		  mutually-authenticated TLS 1.3 server (GrpcMtlsServer) rather than the
		  auto-configured one.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-grpc-server</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-r2dbc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>r2dbc-postgresql</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>com.zaxxer</groupId>
			<artifactId>HikariCP</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.bouncycastle</groupId>
			<artifactId>bcpkix-jdk18on</artifactId>
			<version>${bouncycastle.version}</version>
		</dependency>
		<dependency>
			<groupId>org.bouncycastle</groupId>
			<artifactId>bcprov-jdk18on</artifactId>
			<version>${bouncycastle.version}</version>
		</dependency>
		<dependency>
			<groupId>org.bouncycastle</groupId>
			<artifactId>bcutil-jdk18on</artifactId>
			<version>${bouncycastle.version}</version>
		</dependency>

		<dependency>
			<groupId>com.google.re2j</groupId>
			<artifactId>re2j</artifactId>
			<version>${re2j.version}</version>
		</dependency>

		<!--
		  The `netty-nio-client` exclusion is the same hazard the Azure block below
		  describes: it is Netty-4.1-compiled and this service runs the Boot-managed
		  Netty 4.2, whose `netty-codec` split would break it on first use. Nothing
		  here builds an async AWS client, so it is never loaded - but "never loaded"
		  is a property of today's code, and the artifact is one `AsyncClient` away
		  from being the default transport. Excluding it from `kms` alone would be
		  theatre while this dependency still supplied it.

		  It takes `io.netty:netty-codec` and its `-protobuf`/`-marshalling` siblings
		  out of the runtime tree with it, since nothing else pulls them. That is
		  safe rather than incidental: on Netty 4.2 `netty-codec` is an empty
		  aggregator, the real classes having moved to `netty-codec-base` and
		  `netty-codec-compression`, which reactor-netty brings in on its own.
		-->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
			<exclusions>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>netty-nio-client</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<!--
		  The three exclusions are load-bearing, not tidying:

		  * `azure-core-http-netty` is built against Netty 4.1 and reactor-netty
		    1.2; this service runs Netty 4.2 and reactor-netty 1.3 (Boot-managed,
		    and Boot's dependencyManagement wins). Netty 4.2 split `netty-codec`
		    into `netty-codec-base`/`-compression`, so the 4.1-compiled client
		    would fail on its first vault call - i.e. exactly while signing a
		    certificate. It also drags in netty-tcnative-boringssl-static, and
		    BoringSSL is an OpenSSL fork this build does not admit. The JDK
		    HttpClient provider replaces it: no native code, no second Netty.
		  * `msal4j-persistence-extension` and JNA exist only for the *persistent*
		    token cache (DPAPI / libsecret). Nothing here enables
		    TokenCachePersistenceOptions, and JNA is the only dual-licensed
		    (LGPL-2.1-or-later OR Apache-2.0) artifact the tree would acquire.
		    AzureCredentialsSmokeTest constructs the credential chain for real, so
		    a class these exclusions actually removed from a live path fails the
		    build rather than a vault call months later.
		-->
		<dependency>
			<groupId>com.azure</groupId>
			<artifactId>azure-security-keyvault-keys</artifactId>
			<exclusions>
				<exclusion>
					<groupId>com.azure</groupId>
					<artifactId>azure-core-http-netty</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>com.azure</groupId>
			<artifactId>azure-identity</artifactId>
			<exclusions>
				<exclusion>
					<groupId>com.azure</groupId>
					<artifactId>azure-core-http-netty</artifactId>
				</exclusion>
				<exclusion>
					<groupId>com.microsoft.azure</groupId>
					<artifactId>msal4j-persistence-extension</artifactId>
				</exclusion>
				<exclusion>
					<groupId>net.java.dev.jna</groupId>
					<artifactId>jna</artifactId>
				</exclusion>
				<exclusion>
					<groupId>net.java.dev.jna</groupId>
					<artifactId>jna-platform</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>com.azure</groupId>
			<artifactId>azure-core-http-jdk-httpclient</artifactId>
		</dependency>

		<!--
		  `apache5-client` arrives transitively already, but at runtime scope and by
		  accident of another module's dependencies. AwsKmsSignerFactory names the
		  class directly to pin the transport, so it is declared here: a compile-scope
		  dependency of a class that imports it, rather than a transitive that a
		  future exclusion elsewhere could remove without failing this build.

		  `netty-nio-client` is excluded for the reason given on `s3` above; both
		  paths have to be closed for either to mean anything.
		-->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>kms</artifactId>
			<exclusions>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>netty-nio-client</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>apache5-client</artifactId>
		</dependency>

		<dependency>
			<groupId>io.opentelemetry</groupId>
			<artifactId>opentelemetry-sdk</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.projectreactor</groupId>
			<artifactId>reactor-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-vault</artifactId>
			<scope>test</scope>
		</dependency>
		<!--
		  LocalStack container for the aws_kms CA backend: its KMS does real
		  asymmetric key generation and real ECDSA signing over the real AWS
		  protocol, so the integration tests drive the genuine SDK - request
		  signing, endpoint resolution, credential chain, DER response handling -
		  instead of a double of our own signer seam. Managed by the
		  Boot-imported testcontainers BOM (2.x).
		-->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-localstack</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.grpc</groupId>
			<artifactId>grpc-inprocess</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>net.jqwik</groupId>
			<artifactId>jqwik</artifactId>
			<version>${jqwik.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.opentelemetry</groupId>
			<artifactId>opentelemetry-sdk-testing</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!--
			  protoc version + the protoc-gen-grpc-java plugin + the `generate`
			  execution are all inherited from spring-boot-starter-parent's plugin
			  management. We only redirect the proto source root at the FROZEN
			  contracts so `import "sessionlayer/controlplane/v1/common.proto"`
			  resolves.
			-->
			<plugin>
				<groupId>io.github.ascopes</groupId>
				<artifactId>protobuf-maven-plugin</artifactId>
				<configuration>
					<sourceDirectories>
						<sourceDirectory>${project.basedir}/contracts/proto</sourceDirectory>
					</sourceDirectories>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.openapitools</groupId>
				<artifactId>openapi-generator-maven-plugin</artifactId>
				<version>${openapi-generator.version}</version>
				<executions>
					<execution>
						<id>generate-controlplane-api</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>generate</goal>
						</goals>
						<configuration>
							<inputSpec>${project.basedir}/contracts/openapi/openapi.yaml</inputSpec>
							<generatorName>spring</generatorName>
							<library>spring-boot</library>
							<apiPackage>io.sessionlayer.controlplane.api</apiPackage>
							<modelPackage>io.sessionlayer.controlplane.api.model</modelPackage>
							<output>${project.build.directory}/generated-sources/openapi</output>
							<generateApiTests>false</generateApiTests>
							<generateApiDocumentation>false</generateApiDocumentation>
							<generateModelTests>false</generateModelTests>
							<generateModelDocumentation>false</generateModelDocumentation>
							<configOptions>
								<interfaceOnly>true</interfaceOnly>
								<reactive>true</reactive>
								<useSpringBoot3>true</useSpringBoot3>
								<useJakartaEe>true</useJakartaEe>
								<useTags>true</useTags>
								<useResponseEntity>true</useResponseEntity>
								<useBeanValidation>true</useBeanValidation>
								<performBeanValidation>false</performBeanValidation>
								<openApiNullable>false</openApiNullable>
								<annotationLibrary>none</annotationLibrary>
								<documentationProvider>none</documentationProvider>
								<hideGenerationTimestamp>true</hideGenerationTimestamp>
								<sourceFolder>src/main/java</sourceFolder>
							</configOptions>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>${spotless-maven-plugin.version}</version>
				<configuration>
					<java>
						<includes>
							<include>src/main/java/**/*.java</include>
							<include>src/test/java/**/*.java</include>
						</includes>
						<importOrder/>
						<removeUnusedImports>
							<engine>cleanthat-javaparser-unnecessaryimport</engine>
						</removeUnusedImports>
						<!--
						  Eclipse JDT formatter (Spring's tab-indented house style). We
						  deliberately do NOT use palantir-java-format / google-java-format:
						  both reach into com.sun.tools.javac internals that changed in
						  JDK 25 (NoSuchMethodError on Log$DeferredDiagnosticHandler), and
						  no released version supports JDK 25 yet. The Eclipse JDT engine
						  has an independent parser and runs on any JDK. Swap back to a
						  google-style formatter once one supports JDK 25.
						-->
						<eclipse/>
						<trimTrailingWhitespace/>
						<endWithNewline/>
					</java>
				</configuration>
				<executions>
					<execution>
						<id>spotless-check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<version>${maven-checkstyle-plugin.version}</version>
				<dependencies>
					<dependency>
						<groupId>com.puppycrawl.tools</groupId>
						<artifactId>checkstyle</artifactId>
						<version>${checkstyle.version}</version>
					</dependency>
				</dependencies>
				<configuration>
					<configLocation>${project.basedir}/config/checkstyle/checkstyle.xml</configLocation>
					<sourceDirectories>
						<sourceDirectory>${project.basedir}/src/main/java</sourceDirectory>
					</sourceDirectories>
					<testSourceDirectories>
						<testSourceDirectory>${project.basedir}/src/test/java</testSourceDirectory>
					</testSourceDirectories>
					<includeTestSourceDirectory>true</includeTestSourceDirectory>
					<consoleOutput>true</consoleOutput>
					<failsOnError>true</failsOnError>
					<failOnViolation>true</failOnViolation>
					<violationSeverity>warning</violationSeverity>
				</configuration>
				<executions>
					<execution>
						<id>checkstyle-check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<executions>
					<execution>
						<id>integration-tests</id>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.graalvm.buildtools</groupId>
				<artifactId>native-maven-plugin</artifactId>
			</plugin>

			<!--
			  Declared WITHOUT a lifecycle-bound execution so it stays out of the
			  fast `gate` build; release.yml invokes `makeAggregateBom` explicitly.
			  outputReproducible strips the nondeterministic metadata.timestamp and
			  serialNumber so the SBOM double-builds to an identical digest.
			-->
			<plugin>
				<groupId>org.cyclonedx</groupId>
				<artifactId>cyclonedx-maven-plugin</artifactId>
				<version>${cyclonedx-maven-plugin.version}</version>
				<configuration>
					<schemaVersion>1.6</schemaVersion>
					<outputFormat>json</outputFormat>
					<projectType>application</projectType>
					<outputReproducible>true</outputReproducible>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
